# TickrMeter Firmware

## How to install

1. git pull the project from Platformio: `F1 -> Git clone -> Paste the url of this page`
2. Copy `src\secrets.h.sample` to `src\secrets.h` and update the config elements (this file is in `.gitignore` and will not be pushed to github).
3. Upload :)