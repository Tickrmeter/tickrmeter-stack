{
  "name": "tickrmeter-handler",
  "version": "3.3",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf -rf build && mkdir build",
    "build": "tsc && npm run build:copy",
    "build:deploy_old": "npm run clean && npm run build && npm run update_coingecko && pm2 start build/index.js --time --name tickrmeter-handler && pm2 start build/device_boot.js --time --name tickrmeter-device-boot && pm2 start build/cron_jobs.js --time --name tickrmeter-cron-jobs",
    "build:deploy": "npm run clean && npm run build && npm run update_coingecko && pm2 start ecosystem.config.js --env production",
    "build:copy": "cp configs/config_live.ini build/conf/config.ini && cp package.json build/package.json",
    "build:dev": "tsc && npm run build:copy:dev",
    "build:copy:dev": "cp configs/config.ini build/conf/config.ini && cp package.json build/package.json",
    "build:deploy:dev": "npm run clean && npm run build:dev && npm run update_coingecko && pm2 start build/index.js --time --name tickrmeter-handler && pm2 start build/device_boot.js --time --name tickrmeter-device-boot && pm2 start build/cron_jobs.js --time --name tickrmeter-cron-jobs",
    "deploy:dev": "pm2 stop 1 2 & npm run build:deploy:dev",
    "build:live": "npm run clean && tsc && npm run build:live:copy && mkdir build/uploads && mkdir build/uploads/firmwares",
    "build:live:copy": "cp configs/config_live.ini build/conf/config.ini && cp package.json build/package.json",
    "start:debug": "nodemon --config nodemon.json --inspect-brk src/index.ts",
    "deploy": "pm2 start index.js --time",
    "deploy:live:handler": "pm2 stop tickrmeter-handler 3 4 && npm run build:deploy",
    "lint": "tslint -c tslint.json -p tsconfig.json --fix",
    "start": "nodemon --config nodemon.json src/index.ts",
    "bootscript": "nodemon --config nodemon_boot.json",
    "upload": "sudo scp -i /mnt/c/Users/uzair/Desktop/EPaperStock/TickrMeter-Live.pem -r build/* ubuntu@18.189.133.63:~/tickrmeter-backend",
    "update_coingecko": "node update_coingecko.js",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://uzair-ali@bitbucket.org/utflabs/tickrmeter-handler.git"
  },
  "author": "Uzair Ali",
  "license": "ISC",
  "homepage": "https://bitbucket.org/utflabs/epaper-stock-price#readme",
  "dependencies": {
    "@resvg/resvg-js": "^2.6.0",
    "axios": "^1.3.4",
    "ini": "^3.0.1",
    "ioredis": "^5.3.2",
    "luxon": "^3.3.0",
    "moment": "^2.29.1",
    "mongodb": "^5.1.0",
    "mongoose": "^7.0.2",
    "mqtt": "^4.3.7",
    "node-cron": "^3.0.3",
    "workerpool": "^9.2.0",
    "xmldom": "^0.6.0"
  },
  "devDependencies": {
    "@jest/globals": "^29.6.3",
    "@types/ini": "^1.3.31",
    "@types/jest": "^29.5.4",
    "@types/luxon": "^3.2.0",
    "@types/node": "^18.15.3",
    "jest": "^29.6.3",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.1.2",
    "tslint": "^6.1.3",
    "typescript": "^5.0.2"
  }
}
